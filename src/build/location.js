class Location {
    constructor(id, name, user_id) {
        this.id = id;
        this.name = name;
        this.user_id = user_id;
        // Location.all.push(this);
      }
   
}

// Location.all = [];